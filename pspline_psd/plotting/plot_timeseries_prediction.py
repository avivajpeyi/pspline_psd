import numpy as np
from typing import Callable
import matplotlib.pyplot as plt

def plot_timeseries_prediction(data:np.ndarray, model:Callable):
    """
    Plot the initial path and the preceding predictive densities
    """
    pass
